# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
dist/
*.egg-info/
*.egg

# Virtual environments
venv/
env/
.venv/
ENV/

# Environment variables / secrets
.env
.env.*
!.env.example
config_local.py
instance/

# Flask
*.log
flask_session/

# Database
*.db
*.sqlite3

# FAISS index files (generated, not committed)
data/processed/*.faiss
data/processed/*_meta.pkl

# Downloaded datasets (large, not committed)
data/raw/
*.csv
*.parquet

# IDE / editor
.vscode/
.idea/
*.swp
.DS_Store

# Jupyter
.ipynb_checkpoints/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Piston / Docker local runtime data
piston_data/
cookies.txt

# Demo-day exception: commit the built index so a fresh clone doesn't need
# to reclone roadmap.sh + re-embed to get roadmap generation working.
!data/processed/faiss_index.faiss
!data/processed/faiss_index_meta.pkl
